spring.application.name=config-service
server.port=8000

spring.profiles.active=native
# En local  : file:///home/user/IdeaProjects/MicrofinanceHub/cloud-config/
# En Docker : /workspace/cloud-config (monté en volume, voir docker-compose.yml)
spring.cloud.config.server.native.search-locations=${CONFIG_SEARCH_LOCATION:file://${user.home}/IdeaProjects/MicrofinanceHub/cloud-config/}

# Charger le .env depuis la racine du projet (local seulement — Docker utilise les env vars directement)
dotenv.directory=${DOTENV_DIR:../}
